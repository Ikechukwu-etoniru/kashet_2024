import 'package:flutter/material.dart';

class MyPadding {
  static const textFieldContentPadding = EdgeInsets.all(10);
  static const textFieldDbContentPadding = EdgeInsets.all(10);
  static const screenPadding =
      EdgeInsets.symmetric(vertical: 15, horizontal: 15);
}
